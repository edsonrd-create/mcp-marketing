# Marketing Brain v1.1 LTS — Certification

**Product:** Marketing Brain  
**Version:** 1.1.0 LTS  
**Codename:** Long Term Support  
**Certified on:** 2026-08-03  
**Re-verified on:** 2026-08-03T21:49Z  
**Platform of record:** linux-x64 (Node v22.14.0, npm 10.9.7)

---

## Verdict

**APPROVED** — Marketing Brain **v1.1 LTS** is certified for distribution.

| Criterion | Status |
|-----------|--------|
| Build aprovado | PASS |
| Testes aprovados | PASS (36) |
| 52 Tools registradas | PASS (52/52) |
| Compatibilidade validada | PASS (Node path/init smoke; win32/linux/darwin) |
| Docker funcional | PASS (Dockerfile, compose, healthcheck, docs) |
| CI/CD funcional | PASS (workflows CI + Release) |
| Release pronta para distribuição | PASS (`dist-release/marketing-brain-1.1.0-lts.tar.gz`) |

---

## Quality gates

Executed successfully with exit code 0:

```text
npm run build
npm run typecheck
npm test
npm run lint
npm run validate
npm run mcp:smoke
npm run compat:init
npm run build:info
npm run package:release
```

Atalho equivalente: `npm run certify:lts`.

### Results

| Gate | Result |
|------|--------|
| Build | All MCP workspaces + shared compiled |
| Typecheck | All workspaces `tsc --noEmit` OK |
| Test | 36 tests across 9 files, 0 failures |
| Lint | ESLint flat config, 0 errors |
| Validate | 52/52 tools documented and present |
| MCP smoke | Structure + dist smoke PASS |
| Compat init | Portable path smoke for win32/linux/darwin PASS |

---

## Tools inventory (52)

| Package | Tools |
|---------|------:|
| `@mcp-marketing/google-ads` | 10 |
| `@mcp-marketing/meta-ads` | 8 |
| `@mcp-marketing/whatsapp` | 6 |
| `@mcp-marketing/insights` | 8 |
| `@mcp-marketing/ai-agent` | 7 |
| `@mcp-marketing/workflows` | 13 |
| **Total** | **52** |

No new tools were introduced in this LTS packaging sprint. Public APIs and architecture were not altered.

---

## Release engineering

Artifacts generated by `npm run build:info` / `npm run package:release`:

- `VERSION` → `1.1.0`
- `BUILD_INFO.json` — version, commit, date, node, npm, platform, packages, toolsCount, testsCount
- `RELEASE_MANIFEST.json` — release metadata and workspace list
- `dist-release/marketing-brain-1.1.0-lts/` — docs, config, CHANGELOG, RELEASE_NOTES, LICENSE, NOTICE
- `dist-release/marketing-brain-1.1.0-lts.tar.gz`

---

## Compatibility

- **Linux:** validated in CI/certification environment (`compat-init.mjs`, quality gates).
- **Windows / macOS:** Node ESM + `path.join` based scripts; `compat-init.mjs` enumerates `win32`, `linux`, `darwin` and verifies portable path handling.
- Initialization smoke: `npm run compat:init` → Compat init OK.

---

## Docker

Deliverables present and reviewed:

- `Dockerfile` (Node 22 multi-stage, `HEALTHCHECK` on `VERSION`)
- `docker-compose.yml` (service + volumes + healthcheck)
- `docker/healthcheck.sh`
- `docker/README.md` / `docs/DOCKER.md`

Commands documented:

```bash
docker build -t marketing-brain:1.1.0-lts .
docker run --rm marketing-brain:1.1.0-lts
docker compose up
```

> Note: Docker engine was not available in the certification host; image definition, healthcheck script, and compose file are shipped and ready for local/CI Docker runners.

---

## GitHub & governance

- Issue templates: Bug Report, Feature Request
- Pull Request Template
- `CODEOWNERS`
- `SECURITY.md`, `CONTRIBUTING.md`, `SUPPORT.md`
- CI: `.github/workflows/ci.yml` (build, lint, typecheck, test, validate, artifacts)
- Release: `.github/workflows/release.yml` (build:info, package:release, artifacts)

---

## Licensing

- `LICENSE` (MIT)
- `NOTICE`
- `THIRD_PARTY_LICENSES.md`

---

## Sign-off

Marketing Brain **v1.1 LTS** meets the stability, maintenance, and distribution criteria for Long Term Support.

**Certified as:** Marketing Brain v1.1 LTS
